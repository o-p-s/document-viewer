<!DOCTYPE html>
<html>
<head>
    <meta charset="utf-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge">
    <title>My Drive</title>
    <meta name="viewport" content="width=device-width, initial-scale=1">
    <link rel="stylesheet" type="text/css" media="screen" href="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/css/bootstrap.min.css" />
    <style type="text/css">a{text-decoration: none; color:white;}</style>
</head>
<body>
    <div class="container-fluid text-center" style="background-color: #84919c; color:white">
        <form enctype="multipart/form-data" class="p-5 "> 
            <input type="file" name="fileupload" value="fileupload" id="fileupload"> 
            <label for="fileupload"> Select a file to upload</label> 
            <input  id="upload" type="submit" class="bg-success pb-1 pr-2 border-0" value="submit"> 
        </form>

        <div class="row text-center pt-5 pb-5"> 
                <% files.forEach(function(f){ %>
                   <div id="<%= f.id %>" class="col-md-3" style="margin-bottom:3vh"><figure>
                    <% if(f.name.indexOf('.') > 0) { %>
                        <a href="/drive/download?id=<%=f.id%>&name=<%=f.name%>&at=<%=token%>"><img style="height: 10vh" src="file-icon.ico"/> <figcaption><%= f.name %></figcaption></a>
                    <% } 
                    else {%>
                        <a href="/drive/folder?id=<%=f.id%>&at=<%=token%>" ><img style="height: 10vh;" src="folder-icon.ico"/> <figcaption><%= f.name %></figcaption></a>
                    <% } %>
                    </figure></div>
                <% }); %>
        </div>
    </div>

</body>
<script src="https://code.jquery.com/jquery-3.3.1.min.js" integrity="sha256-FgpCb/KJQlLNfOu91ta32o/NMZxltwRo8QtmkMRdAu8=" crossorigin="anonymous"></script>
<script src="https://stackpath.bootstrapcdn.com/bootstrap/4.1.3/js/bootstrap.min.js"></script>
<script>
    $(document).ready(()=>{ 
        window.localStorage.setItem('token',(new URL(document.location)).searchParams.get('at'));
        $('#upload').on('click',()=>{ console.log($('#fileUpload'));
            // $.ajax({
            //     type:"POST",
            //     url:`http://localhost:3000/drive/upload?file=${this.value}`,
            //     success:(data)=>{console.log('uploaded')},
            //     error:(err)=>{console.log(err)},
            //     timeout:3000
            // })
        })
    })
</script>
</html>